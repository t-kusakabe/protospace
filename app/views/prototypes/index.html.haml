.jumbotron
  .container.text-center
    = image_tag("header_logo.png", alt: 'PROTO TYPE - SHARE THE PROTOTYPE', height: '73px')

= render partial: 'prototypes/sort'

#prototypes
  .container
    .row
      .page
        = render @prototypes

= paginate @prototypes
